export function Import() {
  // open up a text box
  // when on paste, parse input
  // store all valid chars
  //  combine with other stuff when char select

  return <div>Importer goes here</div>;
}
