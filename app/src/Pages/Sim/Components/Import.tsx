export function Import() {
  // open up a text box                                     done
  // when on paste, parse input                             done
  // store all valid chars                                  done
  // convert from GOOD to srl magic sauce

  //  combine with other stuff when char select

  return <div>Importer goes here</div>;
}
