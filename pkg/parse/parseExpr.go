package parse

func parseExpr(p *Parser) (parseFn, error) {
	//expression is anything that returns a value
	//could be math (binary expr), a function call, etc...

	//always starts with either an ident or number
	return nil, nil
}
