import React from "react";
import ReactDOM from "react-dom";
import { UI } from "../lib";

ReactDOM.render(
  <React.StrictMode>
    <UI />
  </React.StrictMode>,
  document.getElementById("root")
);
